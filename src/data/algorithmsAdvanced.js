export const algorithmsAdvanced = [
  { id:'merge-sort',title:'Merge Sort',difficulty:'Intermediate',category:'Sorting',desc:'Divide-and-conquer sorting with O(n log n) complexity.',timeComplexity:'O(n log n)',spaceComplexity:'O(n)',steps:['Divide the array into two halves recursively.','Continue dividing until each subarray has one element.','Merge pairs of subarrays in sorted order.','The merge step compares elements from both halves.'],implementations:{
    Python:`def merge_sort(arr):\n    if len(arr) <= 1:\n        return arr\n    mid = len(arr) // 2\n    left = merge_sort(arr[:mid])\n    right = merge_sort(arr[mid:])\n    return merge(left, right)\n\ndef merge(left, right):\n    result, i, j = [], 0, 0\n    while i < len(left) and j < len(right):\n        if left[i] <= right[j]:\n            result.append(left[i]); i += 1\n        else:\n            result.append(right[j]); j += 1\n    result.extend(left[i:])\n    result.extend(right[j:])\n    return result\n\nprint(merge_sort([38, 27, 43, 3, 9, 82, 10]))`,
    JavaScript:`function mergeSort(arr) {\n  if (arr.length <= 1) return arr;\n  const mid = Math.floor(arr.length / 2);\n  const left = mergeSort(arr.slice(0, mid));\n  const right = mergeSort(arr.slice(mid));\n  return merge(left, right);\n}\n\nfunction merge(left, right) {\n  const result = [];\n  let i = 0, j = 0;\n  while (i < left.length && j < right.length) {\n    if (left[i] <= right[j]) result.push(left[i++]);\n    else result.push(right[j++]);\n  }\n  return [...result, ...left.slice(i), ...right.slice(j)];\n}\n\nconsole.log(mergeSort([38, 27, 43, 3, 9, 82, 10]));`,
    'C++':`#include <iostream>\n#include <vector>\nusing namespace std;\n\nvoid merge(vector<int>& arr, int l, int m, int r) {\n    vector<int> left(arr.begin()+l, arr.begin()+m+1);\n    vector<int> right(arr.begin()+m+1, arr.begin()+r+1);\n    int i=0, j=0, k=l;\n    while (i<left.size() && j<right.size())\n        arr[k++] = (left[i]<=right[j]) ? left[i++] : right[j++];\n    while (i<left.size()) arr[k++] = left[i++];\n    while (j<right.size()) arr[k++] = right[j++];\n}\n\nvoid mergeSort(vector<int>& arr, int l, int r) {\n    if (l>=r) return;\n    int m=(l+r)/2;\n    mergeSort(arr,l,m);\n    mergeSort(arr,m+1,r);\n    merge(arr,l,m,r);\n}`,
    Java:`import java.util.Arrays;\n\npublic class MergeSort {\n    static void mergeSort(int[] arr, int l, int r) {\n        if (l >= r) return;\n        int m = (l + r) / 2;\n        mergeSort(arr, l, m);\n        mergeSort(arr, m + 1, r);\n        merge(arr, l, m, r);\n    }\n\n    static void merge(int[] arr, int l, int m, int r) {\n        int[] left = Arrays.copyOfRange(arr, l, m+1);\n        int[] right = Arrays.copyOfRange(arr, m+1, r+1);\n        int i=0, j=0, k=l;\n        while (i<left.length && j<right.length)\n            arr[k++] = (left[i]<=right[j]) ? left[i++] : right[j++];\n        while (i<left.length) arr[k++] = left[i++];\n        while (j<right.length) arr[k++] = right[j++];\n    }\n}`}},
  { id:'quick-sort',title:'Quick Sort',difficulty:'Intermediate',category:'Sorting',desc:'Partition-based sorting using a pivot element.',timeComplexity:'O(n log n) avg',spaceComplexity:'O(log n)',steps:['Choose a pivot element from the array.','Partition: move smaller elements left, larger right.','Recursively sort the left and right partitions.','Average case is O(n log n), worst case O(n²).'],implementations:{
    Python:`def quick_sort(arr):\n    if len(arr) <= 1:\n        return arr\n    pivot = arr[len(arr) // 2]\n    left = [x for x in arr if x < pivot]\n    middle = [x for x in arr if x == pivot]\n    right = [x for x in arr if x > pivot]\n    return quick_sort(left) + middle + quick_sort(right)\n\nprint(quick_sort([3, 6, 8, 10, 1, 2, 1]))`,
    JavaScript:`function quickSort(arr) {\n  if (arr.length <= 1) return arr;\n  const pivot = arr[Math.floor(arr.length / 2)];\n  const left = arr.filter(x => x < pivot);\n  const middle = arr.filter(x => x === pivot);\n  const right = arr.filter(x => x > pivot);\n  return [...quickSort(left), ...middle, ...quickSort(right)];\n}\n\nconsole.log(quickSort([3, 6, 8, 10, 1, 2, 1]));`,
    'C++':`#include <iostream>\n#include <vector>\nusing namespace std;\n\nint partition(vector<int>& arr, int low, int high) {\n    int pivot = arr[high], i = low - 1;\n    for (int j = low; j < high; j++)\n        if (arr[j] < pivot) swap(arr[++i], arr[j]);\n    swap(arr[i+1], arr[high]);\n    return i+1;\n}\n\nvoid quickSort(vector<int>& arr, int low, int high) {\n    if (low < high) {\n        int pi = partition(arr, low, high);\n        quickSort(arr, low, pi-1);\n        quickSort(arr, pi+1, high);\n    }\n}`,
    Java:`public class QuickSort {\n    static int partition(int[] arr, int low, int high) {\n        int pivot = arr[high], i = low - 1;\n        for (int j = low; j < high; j++) {\n            if (arr[j] < pivot) {\n                int t = arr[++i]; arr[i] = arr[j]; arr[j] = t;\n            }\n        }\n        int t = arr[i+1]; arr[i+1] = arr[high]; arr[high] = t;\n        return i+1;\n    }\n    static void quickSort(int[] arr, int low, int high) {\n        if (low < high) {\n            int pi = partition(arr, low, high);\n            quickSort(arr, low, pi-1);\n            quickSort(arr, pi+1, high);\n        }\n    }\n}`}},
  { id:'linked-list',title:'Linked List',difficulty:'Intermediate',category:'Data Structures',desc:'Dynamic data structure with nodes connected by pointers.',timeComplexity:'O(1) insert, O(n) search',spaceComplexity:'O(n)',steps:['Each node stores data and a reference to the next node.','The head points to the first node in the list.','Insertion at head is O(1), at tail is O(n).','Unlike arrays, no need for contiguous memory.'],implementations:{
    Python:`class Node:\n    def __init__(self, data):\n        self.data = data\n        self.next = None\n\nclass LinkedList:\n    def __init__(self):\n        self.head = None\n\n    def append(self, data):\n        new_node = Node(data)\n        if not self.head:\n            self.head = new_node\n            return\n        current = self.head\n        while current.next:\n            current = current.next\n        current.next = new_node\n\n    def display(self):\n        current = self.head\n        while current:\n            print(current.data, end=" -> ")\n            current = current.next\n        print("None")\n\nll = LinkedList()\nll.append(1); ll.append(2); ll.append(3)\nll.display()  # 1 -> 2 -> 3 -> None`,
    JavaScript:`class Node {\n  constructor(data) {\n    this.data = data;\n    this.next = null;\n  }\n}\n\nclass LinkedList {\n  constructor() { this.head = null; }\n  append(data) {\n    const node = new Node(data);\n    if (!this.head) { this.head = node; return; }\n    let current = this.head;\n    while (current.next) current = current.next;\n    current.next = node;\n  }\n  display() {\n    let current = this.head;\n    const parts = [];\n    while (current) { parts.push(current.data); current = current.next; }\n    console.log(parts.join(" -> ") + " -> null");\n  }\n}`,
    'C++':`#include <iostream>\nusing namespace std;\n\nstruct Node {\n    int data;\n    Node* next;\n    Node(int d) : data(d), next(nullptr) {}\n};\n\nclass LinkedList {\n    Node* head = nullptr;\npublic:\n    void append(int data) {\n        Node* node = new Node(data);\n        if (!head) { head = node; return; }\n        Node* curr = head;\n        while (curr->next) curr = curr->next;\n        curr->next = node;\n    }\n    void display() {\n        Node* curr = head;\n        while (curr) { cout << curr->data << " -> "; curr = curr->next; }\n        cout << "nullptr" << endl;\n    }\n};`,
    Java:`public class LinkedListDemo {\n    static class Node {\n        int data; Node next;\n        Node(int d) { data = d; next = null; }\n    }\n    Node head;\n    void append(int data) {\n        Node node = new Node(data);\n        if (head == null) { head = node; return; }\n        Node curr = head;\n        while (curr.next != null) curr = curr.next;\n        curr.next = node;\n    }\n    void display() {\n        Node curr = head;\n        while (curr != null) { System.out.print(curr.data + " -> "); curr = curr.next; }\n        System.out.println("null");\n    }\n}`}},
  { id:'binary-tree-traversal',title:'Binary Tree Traversal',difficulty:'Intermediate',category:'Trees',desc:'Visit all nodes in a binary tree systematically.',timeComplexity:'O(n)',spaceComplexity:'O(h) where h = height',steps:['Inorder: Left → Root → Right (gives sorted order in BST).','Preorder: Root → Left → Right (useful for copying trees).','Postorder: Left → Right → Root (useful for deletion).','Each traversal visits every node exactly once.'],implementations:{
    Python:`class TreeNode:\n    def __init__(self, val):\n        self.val = val\n        self.left = None\n        self.right = None\n\ndef inorder(node):\n    if node:\n        inorder(node.left)\n        print(node.val, end=" ")\n        inorder(node.right)\n\ndef preorder(node):\n    if node:\n        print(node.val, end=" ")\n        preorder(node.left)\n        preorder(node.right)\n\nroot = TreeNode(1)\nroot.left = TreeNode(2)\nroot.right = TreeNode(3)\ninorder(root)    # 2 1 3\nprint()\npreorder(root)   # 1 2 3`,
    JavaScript:`class TreeNode {\n  constructor(val) {\n    this.val = val;\n    this.left = null;\n    this.right = null;\n  }\n}\n\nfunction inorder(node, result = []) {\n  if (node) {\n    inorder(node.left, result);\n    result.push(node.val);\n    inorder(node.right, result);\n  }\n  return result;\n}\n\nconst root = new TreeNode(1);\nroot.left = new TreeNode(2);\nroot.right = new TreeNode(3);\nconsole.log(inorder(root)); // [2, 1, 3]`,
    'C++':`#include <iostream>\nusing namespace std;\n\nstruct TreeNode {\n    int val;\n    TreeNode *left, *right;\n    TreeNode(int v) : val(v), left(nullptr), right(nullptr) {}\n};\n\nvoid inorder(TreeNode* node) {\n    if (!node) return;\n    inorder(node->left);\n    cout << node->val << " ";\n    inorder(node->right);\n}\n\nint main() {\n    TreeNode* root = new TreeNode(1);\n    root->left = new TreeNode(2);\n    root->right = new TreeNode(3);\n    inorder(root);  // 2 1 3\n}`,
    Java:`public class BinaryTree {\n    static class TreeNode {\n        int val; TreeNode left, right;\n        TreeNode(int v) { val = v; }\n    }\n    static void inorder(TreeNode node) {\n        if (node == null) return;\n        inorder(node.left);\n        System.out.print(node.val + " ");\n        inorder(node.right);\n    }\n    public static void main(String[] args) {\n        TreeNode root = new TreeNode(1);\n        root.left = new TreeNode(2);\n        root.right = new TreeNode(3);\n        inorder(root); // 2 1 3\n    }\n}`}},
  { id:'graph-bfs',title:'Graph BFS',difficulty:'Advanced',category:'Graphs',desc:'Breadth-first search for exploring graphs level by level.',timeComplexity:'O(V + E)',spaceComplexity:'O(V)',steps:['Start from a source node, add it to a queue.','Visit all neighbors of the current node first.','Then visit neighbors of those neighbors (next level).','Use a visited set to avoid revisiting nodes.'],implementations:{
    Python:`from collections import deque\n\ndef bfs(graph, start):\n    visited = set([start])\n    queue = deque([start])\n    order = []\n    while queue:\n        node = queue.popleft()\n        order.append(node)\n        for neighbor in graph[node]:\n            if neighbor not in visited:\n                visited.add(neighbor)\n                queue.append(neighbor)\n    return order\n\ngraph = {\n    'A': ['B','C'], 'B': ['A','D','E'],\n    'C': ['A','F'], 'D': ['B'],\n    'E': ['B','F'], 'F': ['C','E']\n}\nprint(bfs(graph, 'A'))`,
    JavaScript:`function bfs(graph, start) {\n  const visited = new Set([start]);\n  const queue = [start];\n  const order = [];\n  while (queue.length) {\n    const node = queue.shift();\n    order.push(node);\n    for (const neighbor of graph[node]) {\n      if (!visited.has(neighbor)) {\n        visited.add(neighbor);\n        queue.push(neighbor);\n      }\n    }\n  }\n  return order;\n}`,
    'C++':`#include <iostream>\n#include <unordered_map>\n#include <unordered_set>\n#include <queue>\n#include <vector>\nusing namespace std;\n\nvector<char> bfs(unordered_map<char,vector<char>>& graph, char start) {\n    unordered_set<char> visited = {start};\n    queue<char> q;\n    q.push(start);\n    vector<char> order;\n    while (!q.empty()) {\n        char node = q.front(); q.pop();\n        order.push_back(node);\n        for (char n : graph[node])\n            if (!visited.count(n)) { visited.insert(n); q.push(n); }\n    }\n    return order;\n}`,
    Java:`import java.util.*;\n\npublic class BFS {\n    static List<Character> bfs(Map<Character,List<Character>> graph, char start) {\n        Set<Character> visited = new HashSet<>(List.of(start));\n        Queue<Character> queue = new LinkedList<>(List.of(start));\n        List<Character> order = new ArrayList<>();\n        while (!queue.isEmpty()) {\n            char node = queue.poll();\n            order.add(node);\n            for (char n : graph.get(node))\n                if (!visited.contains(n)) { visited.add(n); queue.add(n); }\n        }\n        return order;\n    }\n}`}},
  { id:'graph-dfs',title:'Graph DFS',difficulty:'Advanced',category:'Graphs',desc:'Depth-first search for deep exploration of graph paths.',timeComplexity:'O(V + E)',spaceComplexity:'O(V)',steps:['Start from a source node, mark it visited.','Explore as far as possible along each branch.','Backtrack when a dead end is reached.','Can be implemented recursively or with a stack.'],implementations:{
    Python:`def dfs(graph, start, visited=None):\n    if visited is None:\n        visited = set()\n    visited.add(start)\n    print(start, end=" ")\n    for neighbor in graph[start]:\n        if neighbor not in visited:\n            dfs(graph, neighbor, visited)\n\ngraph = {\n    'A': ['B','C'], 'B': ['A','D','E'],\n    'C': ['A','F'], 'D': ['B'],\n    'E': ['B','F'], 'F': ['C','E']\n}\ndfs(graph, 'A')  # A B D E F C`,
    JavaScript:`function dfs(graph, start, visited = new Set()) {\n  visited.add(start);\n  console.log(start);\n  for (const neighbor of graph[start]) {\n    if (!visited.has(neighbor)) {\n      dfs(graph, neighbor, visited);\n    }\n  }\n}`,
    'C++':`#include <iostream>\n#include <unordered_map>\n#include <unordered_set>\n#include <vector>\nusing namespace std;\n\nvoid dfs(unordered_map<char,vector<char>>& graph, char node, unordered_set<char>& visited) {\n    visited.insert(node);\n    cout << node << " ";\n    for (char n : graph[node])\n        if (!visited.count(n)) dfs(graph, n, visited);\n}`,
    Java:`import java.util.*;\n\npublic class DFS {\n    static void dfs(Map<Character,List<Character>> graph, char node, Set<Character> visited) {\n        visited.add(node);\n        System.out.print(node + " ");\n        for (char n : graph.get(node))\n            if (!visited.contains(n)) dfs(graph, n, visited);\n    }\n}`}},
  { id:'dynamic-programming',title:'Dynamic Programming',difficulty:'Advanced',category:'Concepts',desc:'Solve complex problems by breaking them into overlapping subproblems.',timeComplexity:'Varies (often O(n))',spaceComplexity:'O(n)',steps:['Identify overlapping subproblems in the recursion.','Store results of subproblems (memoization or tabulation).','Bottom-up: build solution from smallest subproblems.','Dramatically reduces time from exponential to polynomial.'],implementations:{
    Python:`def fib_memo(n, memo={}):\n    if n in memo: return memo[n]\n    if n <= 1: return n\n    memo[n] = fib_memo(n-1) + fib_memo(n-2)\n    return memo[n]\n\ndef fib_tab(n):\n    if n <= 1: return n\n    dp = [0] * (n+1)\n    dp[1] = 1\n    for i in range(2, n+1):\n        dp[i] = dp[i-1] + dp[i-2]\n    return dp[n]\n\nprint(fib_memo(10))  # 55\nprint(fib_tab(10))   # 55`,
    JavaScript:`function fibMemo(n, memo = {}) {\n  if (n in memo) return memo[n];\n  if (n <= 1) return n;\n  memo[n] = fibMemo(n-1, memo) + fibMemo(n-2, memo);\n  return memo[n];\n}\n\nfunction fibTab(n) {\n  if (n <= 1) return n;\n  const dp = new Array(n+1).fill(0);\n  dp[1] = 1;\n  for (let i = 2; i <= n; i++) dp[i] = dp[i-1] + dp[i-2];\n  return dp[n];\n}\n\nconsole.log(fibMemo(10)); // 55\nconsole.log(fibTab(10));  // 55`,
    'C++':`#include <iostream>\n#include <vector>\n#include <unordered_map>\nusing namespace std;\n\nunordered_map<int,long long> memo;\nlong long fibMemo(int n) {\n    if (memo.count(n)) return memo[n];\n    if (n <= 1) return n;\n    return memo[n] = fibMemo(n-1) + fibMemo(n-2);\n}\n\nlong long fibTab(int n) {\n    if (n <= 1) return n;\n    vector<long long> dp(n+1, 0);\n    dp[1] = 1;\n    for (int i=2; i<=n; i++) dp[i] = dp[i-1]+dp[i-2];\n    return dp[n];\n}`,
    Java:`import java.util.HashMap;\n\npublic class DP {\n    static HashMap<Integer,Long> memo = new HashMap<>();\n    static long fibMemo(int n) {\n        if (memo.containsKey(n)) return memo.get(n);\n        if (n <= 1) return n;\n        long result = fibMemo(n-1) + fibMemo(n-2);\n        memo.put(n, result);\n        return result;\n    }\n    static long fibTab(int n) {\n        if (n <= 1) return n;\n        long[] dp = new long[n+1];\n        dp[1] = 1;\n        for (int i=2; i<=n; i++) dp[i] = dp[i-1]+dp[i-2];\n        return dp[n];\n    }\n}`}},
];
